{
    "name": "euchre-server",
    "version": "1.0.0",
    "scripts": {
        "clean": "npx rimraf dist",
        "build": "tsc -p tsconfig.json",
        "test": "jest --preset ts-jest --coverage --coverageReporters=text"
    },
    "devDependencies": {
        "@types/aws-lambda": "^8.10.160",
        "@types/jest": "^30.0.0",
        "@types/uuid": "^10.0.0",
        "aws-sdk-client-mock": "^4.1.0",
        "aws-sdk-client-mock-jest": "^4.1.0",
        "jest": "^30.2.0",
        "ts-jest": "^29.4.6"
    },
    "dependencies": {
        "@aws-sdk/client-apigatewaymanagementapi": "^3.996.0",
        "@aws-sdk/client-dynamodb": "^3.996.0",
        "@aws-sdk/util-dynamodb": "^3.996.0"
    }
}
