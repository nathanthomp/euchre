{
    "name": "euchre",
    "workspaces": [
        "client",
        "infrastructure",
        "server"
    ],
    "scripts": {
        "clean": "npm run clean --workspaces && npx rimraf node_modules",
        "build": "npm run build --workspaces",
        "test": "npm run test --workspace client && npm run test --workspace server",
        "deploy": "npm run build && npm run test && npm run deploy --workspace infrastructure"
    },
    "devDependencies": {
        "rimraf": "^6.1.2",
        "typescript": "^5.9.3"
    }
}